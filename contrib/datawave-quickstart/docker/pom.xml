<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>gov.nsa.datawave</groupId>
        <artifactId>datawave-parent</artifactId>
        <version>7.10.0-SNAPSHOT</version>
        <relativePath>../../../pom.xml</relativePath>
    </parent>
    <artifactId>quickstart</artifactId>
    <packaging>pom</packaging>
    <name>${project.artifactId}</name>
    <properties>
        <dist.accumulo>accumulo-${version.accumulo.tar.gz}-bin.tar.gz</dist.accumulo>
        <dist.hadoop>hadoop-${version.hadoop.tar.gz}.tar.gz</dist.hadoop>
        <dist.maven>apache-maven-${version.maven.tar.gz}-bin.tar.gz</dist.maven>
        <dist.wildfly>wildfly-${version.wildfly.tar.gz}.Final.tar.gz</dist.wildfly>
        <dist.zookeeper>apache-zookeeper-${version.zookeeper.tar.gz}-bin.tar.gz</dist.zookeeper>
        <skipIngest>false</skipIngest>
        <url.accumulo>https://dlcdn.apache.org/accumulo/${version.accumulo.tar.gz}/${dist.accumulo}</url.accumulo>
        <url.hadoop>https://dlcdn.apache.org/hadoop/common/hadoop-${version.hadoop.tar.gz}/${dist.hadoop}</url.hadoop>
        <url.maven>https://dlcdn.apache.org/maven/maven-3/${version.maven.tar.gz}/binaries/${dist.maven}</url.maven>
        <url.wildfly>https://download.jboss.org/wildfly/${version.wildfly.tar.gz}.Final/${dist.wildfly}</url.wildfly>
        <url.zookeeper>https://dlcdn.apache.org/zookeeper/zookeeper-${version.zookeeper.tar.gz}/${dist.zookeeper}</url.zookeeper>
        <!-- These distributions and URLs should match what is listed in each service's bootstrap.sh script -->
        <version.accumulo.tar.gz>2.1.3</version.accumulo.tar.gz>
        <version.hadoop.tar.gz>3.3.6</version.hadoop.tar.gz>
        <version.maven.tar.gz>3.8.8</version.maven.tar.gz>
        <version.wildfly.tar.gz>17.0.1</version.wildfly.tar.gz>
        <version.zookeeper.tar.gz>3.7.2</version.zookeeper.tar.gz>
    </properties>
    <dependencies>
        <dependency>
            <groupId>gov.nsa.datawave</groupId>
            <artifactId>assemble-datawave</artifactId>
            <version>${project.version}</version>
            <type>pom</type>
        </dependency>
        <dependency>
            <groupId>gov.nsa.datawave</groupId>
            <artifactId>assemble-datawave</artifactId>
            <version>${project.version}</version>
            <classifier>dist</classifier>
            <type>tar.gz</type>
        </dependency>
        <dependency>
            <groupId>gov.nsa.datawave.webservices</groupId>
            <artifactId>datawave-ws-deploy-application</artifactId>
            <version>${project.version}</version>
            <type>pom</type>
        </dependency>
        <dependency>
            <groupId>gov.nsa.datawave.webservices</groupId>
            <artifactId>datawave-ws-deploy-application</artifactId>
            <version>${project.version}</version>
            <classifier>${build.env}</classifier>
            <type>tar.gz</type>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <artifactId>maven-clean-plugin</artifactId>
                <version>3.2.0</version>
                <configuration>
                    <filesets>
                        <fileset>
                            <directory>${project.basedir}/../../../</directory>
                            <includes>
                                <include>.maven-dockerignore</include>
                            </includes>
                        </fileset>
                        <fileset>
                            <directory>../bin/services/accumulo</directory>
                            <includes>
                                <include>**/apache-zookeeper-*-bin.tar.gz</include>
                                <include>**/accumulo-*-bin.tar.gz</include>
                                <include>**/zookeeper-*.tar.gz</include>
                                <include>**/accumulo-*.tar.gz</include>
                            </includes>
                        </fileset>
                        <fileset>
                            <directory>../bin/services/datawave</directory>
                            <includes>
                                <include>**/datawave-*-dist.tar.gz</include>
                                <include>**/datawave-ws-deploy-application-*.tar.gz</include>
                                <include>**/wildfly-*.tar.gz</include>
                            </includes>
                        </fileset>
                        <fileset>
                            <directory>../bin/services/hadoop</directory>
                            <includes>
                                <include>**/hadoop-*.tar.gz</include>
                            </includes>
                        </fileset>
                        <fileset>
                            <directory>../bin/services/maven</directory>
                            <includes>
                                <include>**/apache-maven-*-bin.tar.gz</include>
                                <include>**/maven-*.tar.gz</include>
                            </includes>
                        </fileset>
                    </filesets>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>copy-datawave-warehouse</id>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <phase>process-resources</phase>
                        <configuration>
                            <artifact>gov.nsa.datawave:assemble-datawave:${project.version}:tar.gz:dist</artifact>
                            <outputDirectory>${project.basedir}/../bin/services/datawave</outputDirectory>
                        </configuration>
                    </execution>
                    <execution>
                        <id>copy-datawave-webservice</id>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <phase>process-resources</phase>
                        <configuration>
                            <artifact>gov.nsa.datawave.webservices:datawave-ws-deploy-application:${project.version}:tar.gz:${build.env}</artifact>
                            <outputDirectory>${project.basedir}/../bin/services/datawave</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <?SORTPOM IGNORE?>
            <plugin>
                <artifactId>maven-antrun-plugin</artifactId>
                <executions>
                    <execution>
                        <id>rename-datawave-warehouse</id>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <phase>process-resources</phase>
                        <configuration>
                            <target>
                                <move file="${project.basedir}/../bin/services/datawave/assemble-datawave-${project.version}-dist.tar.gz" tofile="${project.basedir}/../bin/services/datawave/datawave-${build.env}-${project.version}-dist.tar.gz" />
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <?SORTPOM RESUME?>
        </plugins>
    </build>
    <profiles>
        <profile>
            <id>docker</id>
            <activation>
                <property>
                    <name>quickstart-docker</name>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>io.fabric8</groupId>
                        <artifactId>docker-maven-plugin</artifactId>
                        <version>0.40.1</version>
                        <configuration>
                            <verbose>true</verbose>
                            <images>
                                <image>
                                    <name>${docker.image.prefix}datawave/quickstart-${build.env}</name>
                                    <build>
                                        <buildOptions>
                                            <memory>8589934592</memory>
                                        </buildOptions>
                                        <cleanup>none</cleanup>
                                        <contextDir>${project.basedir}/../../../</contextDir>
                                        <dockerFile>${project.basedir}/Dockerfile</dockerFile>
                                        <tags>
                                            <tag>latest</tag>
                                            <tag>${project.version}</tag>
                                        </tags>
                                        <args>
                                            <DATAWAVE_COMMIT_ID>${git.commit.id}</DATAWAVE_COMMIT_ID>
                                            <DATAWAVE_BRANCH_NAME>${git.branch}</DATAWAVE_BRANCH_NAME>
                                            <DATAWAVE_BUILD_PROFILE>${build.env}</DATAWAVE_BUILD_PROFILE>
                                            <DATAWAVE_SKIP_INGEST>${skipIngest}</DATAWAVE_SKIP_INGEST>
                                            <DATAWAVE_SKIP_TESTS>true</DATAWAVE_SKIP_TESTS>
                                            <ACCUMULO_URL>${url.accumulo}</ACCUMULO_URL>
                                            <HADOOP_URL>${url.hadoop}</HADOOP_URL>
                                            <MAVEN_URL>${url.maven}</MAVEN_URL>
                                            <WILDFLY_URL>${url.wildfly}</WILDFLY_URL>
                                            <ZOOKEEPER_URL>${url.zookeeper}</ZOOKEEPER_URL>
                                        </args>
                                    </build>
                                </image>
                            </images>
                        </configuration>
                        <executions>
                            <execution>
                                <id>build</id>
                                <goals>
                                    <goal>build</goal>
                                </goals>
                                <phase>install</phase>
                            </execution>
                            <execution>
                                <id>push</id>
                                <goals>
                                    <goal>push</goal>
                                </goals>
                                <phase>deploy</phase>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <artifactId>maven-resources-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>copy-docker-ignore</id>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <phase>process-resources</phase>
                                <configuration>
                                    <outputDirectory>${project.basedir}/../../../</outputDirectory>
                                    <resources>
                                        <resource>
                                            <directory>${project.basedir}</directory>
                                            <includes>
                                                <include>.maven-dockerignore</include>
                                            </includes>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>pl.project13.maven</groupId>
                        <artifactId>git-commit-id-plugin</artifactId>
                        <configuration>
                            <skipPoms>false</skipPoms>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>quickstart-default</id>
            <activation>
                <property>
                    <name>!quickstart-maven</name>
                </property>
            </activation>
            <build>
                <plugins>
                    <!-- the download plugin is beneficial because it caches the downloaded tarballs in the m2 repo -->
                    <plugin>
                        <groupId>com.googlecode.maven-download-plugin</groupId>
                        <artifactId>download-maven-plugin</artifactId>
                        <version>1.6.8</version>
                        <executions>
                            <execution>
                                <id>download-zookeeper</id>
                                <goals>
                                    <goal>wget</goal>
                                </goals>
                                <configuration>
                                    <url>${url.zookeeper}</url>
                                    <outputDirectory>${project.build.outputDirectory}</outputDirectory>
                                </configuration>
                            </execution>
                            <execution>
                                <id>download-accumulo</id>
                                <goals>
                                    <goal>wget</goal>
                                </goals>
                                <configuration>
                                    <url>${url.accumulo}</url>
                                    <outputDirectory>${project.build.outputDirectory}</outputDirectory>
                                </configuration>
                            </execution>
                            <execution>
                                <id>download-hadoop</id>
                                <goals>
                                    <goal>wget</goal>
                                </goals>
                                <configuration>
                                    <url>${url.hadoop}</url>
                                    <outputDirectory>${project.build.outputDirectory}</outputDirectory>
                                </configuration>
                            </execution>
                            <execution>
                                <id>download-maven</id>
                                <goals>
                                    <goal>wget</goal>
                                </goals>
                                <configuration>
                                    <url>${url.maven}</url>
                                    <outputDirectory>${project.build.outputDirectory}</outputDirectory>
                                </configuration>
                            </execution>
                            <execution>
                                <id>download-wildfly</id>
                                <goals>
                                    <goal>wget</goal>
                                </goals>
                                <configuration>
                                    <url>${url.wildfly}</url>
                                    <outputDirectory>${project.build.outputDirectory}</outputDirectory>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <artifactId>maven-resources-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>copy-accumulo-tarballs</id>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <phase>process-resources</phase>
                                <configuration>
                                    <outputDirectory>${project.basedir}/../bin/services/accumulo</outputDirectory>
                                    <resources>
                                        <resource>
                                            <directory>${project.build.outputDirectory}</directory>
                                            <includes>
                                                <include>${dist.zookeeper}</include>
                                                <include>${dist.accumulo}</include>
                                            </includes>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                            <execution>
                                <id>copy-hadoop-tarballs</id>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <phase>process-resources</phase>
                                <configuration>
                                    <outputDirectory>${project.basedir}/../bin/services/hadoop</outputDirectory>
                                    <resources>
                                        <resource>
                                            <directory>${project.build.outputDirectory}</directory>
                                            <includes>
                                                <include>${dist.hadoop}</include>
                                            </includes>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                            <execution>
                                <id>copy-maven-tarballs</id>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <phase>process-resources</phase>
                                <configuration>
                                    <outputDirectory>${project.basedir}/../bin/services/maven</outputDirectory>
                                    <resources>
                                        <resource>
                                            <directory>${project.build.outputDirectory}</directory>
                                            <includes>
                                                <include>${dist.maven}</include>
                                            </includes>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                            <execution>
                                <id>copy-wildfly-tarball</id>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <phase>process-resources</phase>
                                <configuration>
                                    <outputDirectory>${project.basedir}/../bin/services/datawave</outputDirectory>
                                    <resources>
                                        <resource>
                                            <directory>${project.build.outputDirectory}</directory>
                                            <includes>
                                                <include>${dist.wildfly}</include>
                                            </includes>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>quickstart-maven</id>
            <activation>
                <property>
                    <name>quickstart-maven</name>
                </property>
            </activation>
            <dependencies>
                <dependency>
                    <groupId>gov.nsa.datawave.quickstart</groupId>
                    <artifactId>accumulo</artifactId>
                    <version>${version.accumulo.tar.gz}</version>
                    <type>tar.gz</type>
                </dependency>
                <dependency>
                    <groupId>gov.nsa.datawave.quickstart</groupId>
                    <artifactId>hadoop</artifactId>
                    <version>${version.hadoop.tar.gz}</version>
                    <type>tar.gz</type>
                </dependency>
                <dependency>
                    <groupId>gov.nsa.datawave.quickstart</groupId>
                    <artifactId>maven</artifactId>
                    <version>${version.maven.tar.gz}</version>
                    <type>tar.gz</type>
                </dependency>
                <dependency>
                    <groupId>gov.nsa.datawave.quickstart</groupId>
                    <artifactId>wildfly</artifactId>
                    <version>${version.wildfly.tar.gz}</version>
                    <type>tar.gz</type>
                </dependency>
                <dependency>
                    <groupId>gov.nsa.datawave.quickstart</groupId>
                    <artifactId>zookeeper</artifactId>
                    <version>${version.zookeeper.tar.gz}</version>
                    <type>tar.gz</type>
                </dependency>
            </dependencies>
            <build>
                <plugins>
                    <plugin>
                        <artifactId>maven-dependency-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>copy-accumulo-tarball</id>
                                <goals>
                                    <goal>copy</goal>
                                </goals>
                                <phase>process-resources</phase>
                                <configuration>
                                    <artifact>gov.nsa.datawave.quickstart:accumulo:${version.accumulo.tar.gz}:tar.gz</artifact>
                                    <outputDirectory>${project.basedir}/../bin/services/accumulo</outputDirectory>
                                </configuration>
                            </execution>
                            <execution>
                                <id>copy-hadoop-tarball</id>
                                <goals>
                                    <goal>copy</goal>
                                </goals>
                                <phase>process-resources</phase>
                                <configuration>
                                    <artifact>gov.nsa.datawave.quickstart:hadoop:${version.hadoop.tar.gz}:tar.gz</artifact>
                                    <outputDirectory>${project.basedir}/../bin/services/hadoop</outputDirectory>
                                </configuration>
                            </execution>
                            <execution>
                                <id>copy-maven-tarball</id>
                                <goals>
                                    <goal>copy</goal>
                                </goals>
                                <phase>process-resources</phase>
                                <configuration>
                                    <artifact>gov.nsa.datawave.quickstart:maven:${version.maven.tar.gz}:tar.gz</artifact>
                                    <outputDirectory>${project.basedir}/../bin/services/maven</outputDirectory>
                                </configuration>
                            </execution>
                            <execution>
                                <id>copy-wildfly-tarball</id>
                                <goals>
                                    <goal>copy</goal>
                                </goals>
                                <phase>process-resources</phase>
                                <configuration>
                                    <artifact>gov.nsa.datawave.quickstart:wildfly:${version.wildfly.tar.gz}:tar.gz</artifact>
                                    <outputDirectory>${project.basedir}/../bin/services/datawave</outputDirectory>
                                </configuration>
                            </execution>
                            <execution>
                                <id>copy-zookeeper-tarball</id>
                                <goals>
                                    <goal>copy</goal>
                                </goals>
                                <phase>process-resources</phase>
                                <configuration>
                                    <artifact>gov.nsa.datawave.quickstart:zookeeper:${version.zookeeper.tar.gz}:tar.gz</artifact>
                                    <outputDirectory>${project.basedir}/../bin/services/accumulo</outputDirectory>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>