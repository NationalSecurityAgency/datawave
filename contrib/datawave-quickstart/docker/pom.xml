<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>gov.nsa.datawave</groupId>
        <artifactId>datawave-parent</artifactId>
        <version>7.27.0-SNAPSHOT</version>
        <relativePath>../../../pom.xml</relativePath>
    </parent>
    <artifactId>quickstart</artifactId>
    <packaging>pom</packaging>
    <name>${project.artifactId}</name>
    <properties>
        <datawave.quickstart.ingest.test.data.skip>${skipIngest}</datawave.quickstart.ingest.test.data.skip>
        <datawave.quickstart.web.tests.skip>true</datawave.quickstart.web.tests.skip>
        <dist.accumulo>accumulo-${version.accumulo.tar.gz}.tar.gz</dist.accumulo>
        <dist.hadoop>hadoop-${version.hadoop.tar.gz}.tar.gz</dist.hadoop>
        <dist.wildfly>wildfly-${version.wildfly.tar.gz}.tar.gz</dist.wildfly>
        <dist.zookeeper>zookeeper-${version.zookeeper.tar.gz}.tar.gz</dist.zookeeper>
        <skipIngest>false</skipIngest>
        <url.accumulo>file://${DW_CLOUD_PLUGINS}/accumulo/${dist.accumulo}</url.accumulo>
        <url.hadoop>file://${DW_CLOUD_PLUGINS}/hadoop/${dist.hadoop}</url.hadoop>
        <url.wildfly>file://${DW_CLOUD_PLUGINS}/datawave/${dist.wildfly}</url.wildfly>
        <url.zookeeper>file://${DW_CLOUD_PLUGINS}/accumulo/${dist.zookeeper}</url.zookeeper>
        <!-- These distributions and URLs should match what is listed in each service's bootstrap.sh script -->
        <version.accumulo.tar.gz>2.1.3</version.accumulo.tar.gz>
        <version.hadoop.tar.gz>3.3.6</version.hadoop.tar.gz>
        <version.wildfly.tar.gz>17.0.1</version.wildfly.tar.gz>
        <version.zookeeper.tar.gz>3.8.4</version.zookeeper.tar.gz>
    </properties>
    <dependencies>
        <dependency>
            <groupId>gov.nsa.datawave</groupId>
            <artifactId>assemble-datawave</artifactId>
            <version>${project.version}</version>
            <type>pom</type>
        </dependency>
        <dependency>
            <groupId>gov.nsa.datawave</groupId>
            <artifactId>assemble-datawave</artifactId>
            <version>${project.version}</version>
            <classifier>dist</classifier>
            <type>tar.gz</type>
        </dependency>
        <dependency>
            <groupId>gov.nsa.datawave.webservices</groupId>
            <artifactId>datawave-ws-deploy-application</artifactId>
            <version>${project.version}</version>
            <type>pom</type>
        </dependency>
        <dependency>
            <groupId>gov.nsa.datawave.webservices</groupId>
            <artifactId>datawave-ws-deploy-application</artifactId>
            <version>${project.version}</version>
            <classifier>${build.env}</classifier>
            <type>tar.gz</type>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <artifactId>maven-clean-plugin</artifactId>
                <configuration>
                    <filesets>
                        <fileset>
                            <directory>${maven.multiModuleProjectDirectory}</directory>
                            <includes>
                                <include>.jkube-dockerignore</include>
                            </includes>
                        </fileset>
                        <fileset>
                            <directory>../bin/services/accumulo</directory>
                            <includes>
                                <include>**/apache-zookeeper-*-bin.tar.gz</include>
                                <include>**/accumulo-*-bin.tar.gz</include>
                                <include>**/zookeeper-*.tar.gz</include>
                                <include>**/accumulo-*.tar.gz</include>
                            </includes>
                        </fileset>
                        <fileset>
                            <directory>../bin/services/datawave</directory>
                            <includes>
                                <include>**/datawave-*-dist.tar.gz</include>
                                <include>**/datawave-ws-deploy-application-*.tar.gz</include>
                                <include>**/wildfly-*.tar.gz</include>
                            </includes>
                        </fileset>
                        <fileset>
                            <directory>../bin/services/hadoop</directory>
                            <includes>
                                <include>**/hadoop-*.tar.gz</include>
                            </includes>
                        </fileset>
                    </filesets>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>copy-datawave-warehouse</id>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <phase>process-resources</phase>
                        <configuration>
                            <artifact>gov.nsa.datawave:assemble-datawave:${project.version}:tar.gz:dist</artifact>
                            <outputDirectory>${project.basedir}/../bin/services/datawave</outputDirectory>
                        </configuration>
                    </execution>
                    <execution>
                        <id>copy-datawave-webservice</id>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <phase>process-resources</phase>
                        <configuration>
                            <artifact>gov.nsa.datawave.webservices:datawave-ws-deploy-application:${project.version}:tar.gz:${build.env}</artifact>
                            <outputDirectory>${project.basedir}/../bin/services/datawave</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <?SORTPOM IGNORE?>
            <plugin>
                <artifactId>maven-antrun-plugin</artifactId>
                <executions>
                    <execution>
                        <id>rename-datawave-warehouse</id>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <phase>process-resources</phase>
                        <configuration>
                            <target>
                                <move file="${project.basedir}/../bin/services/datawave/assemble-datawave-${project.version}-dist.tar.gz" tofile="${project.basedir}/../bin/services/datawave/datawave-${build.env}-${project.version}-dist.tar.gz" />
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <?SORTPOM RESUME?>
        </plugins>
    </build>
    <profiles>
        <profile>
            <id>docker</id>
            <activation>
                <property>
                    <name>quickstart-docker</name>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>io.github.git-commit-id</groupId>
                        <artifactId>git-commit-id-maven-plugin</artifactId>
                        <configuration>
                            <skipPoms>false</skipPoms>
                        </configuration>
                    </plugin>
                    <plugin>
                        <artifactId>maven-resources-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>copy-docker-ignore</id>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <phase>process-resources</phase>
                                <configuration>
                                    <outputDirectory>${maven.multiModuleProjectDirectory}</outputDirectory>
                                    <resources>
                                        <resource>
                                            <directory>${project.basedir}</directory>
                                            <includes>
                                                <include>.jkube-dockerignore</include>
                                            </includes>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.eclipse.jkube</groupId>
                        <artifactId>kubernetes-maven-plugin</artifactId>
                        <configuration>
                            <verbose>true</verbose>
                            <pushRegistry>${docker.image.prefix}</pushRegistry>
                            <images>
                                <image>
                                    <name>quickstart-${build.env}:${project.version}</name>
                                    <build>
                                        <contextDir>${maven.multiModuleProjectDirectory}</contextDir>
                                        <dockerFile>${project.basedir}/Dockerfile</dockerFile>
                                        <buildOptions>
                                            <memory>8589934592</memory>
                                        </buildOptions>
                                        <args>
                                            <DATAWAVE_COMMIT_ID>${git.commit.id}</DATAWAVE_COMMIT_ID>
                                            <DATAWAVE_BRANCH_NAME>${git.branch}</DATAWAVE_BRANCH_NAME>
                                            <DATAWAVE_BUILD_PROFILE>${build.env}</DATAWAVE_BUILD_PROFILE>
                                            <DATAWAVE_INGEST_TEST_DATA_SKIP>${datawave.quickstart.ingest.test.data.skip}</DATAWAVE_INGEST_TEST_DATA_SKIP>
                                            <DATAWAVE_WEB_TEST_SKIP>${datawave.quickstart.web.tests.skip}</DATAWAVE_WEB_TEST_SKIP>
                                            <ACCUMULO_URL>${url.accumulo}</ACCUMULO_URL>
                                            <HADOOP_URL>${url.hadoop}</HADOOP_URL>
                                            <WILDFLY_URL>${url.wildfly}</WILDFLY_URL>
                                            <ZOOKEEPER_URL>${url.zookeeper}</ZOOKEEPER_URL>
                                        </args>
                                        <assembly>
                                            <name>/</name>
                                        </assembly>
                                    </build>
                                </image>
                            </images>
                        </configuration>
                        <executions>
                            <execution>
                                <id>build</id>
                                <goals>
                                    <goal>build</goal>
                                </goals>
                                <phase>install</phase>
                            </execution>
                            <execution>
                                <id>push</id>
                                <goals>
                                    <goal>push</goal>
                                </goals>
                                <phase>deploy</phase>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>quickstart-maven</id>
            <activation>
                <property>
                    <name>!skipDownload</name>
                </property>
            </activation>
            <dependencies>
                <dependency>
                    <groupId>gov.nsa.datawave.quickstart</groupId>
                    <artifactId>accumulo</artifactId>
                    <version>${version.accumulo.tar.gz}</version>
                    <type>tar.gz</type>
                </dependency>
                <dependency>
                    <groupId>gov.nsa.datawave.quickstart</groupId>
                    <artifactId>hadoop</artifactId>
                    <version>${version.hadoop.tar.gz}</version>
                    <type>tar.gz</type>
                </dependency>
                <dependency>
                    <groupId>gov.nsa.datawave.quickstart</groupId>
                    <artifactId>wildfly</artifactId>
                    <version>${version.wildfly.tar.gz}</version>
                    <type>tar.gz</type>
                </dependency>
                <dependency>
                    <groupId>gov.nsa.datawave.quickstart</groupId>
                    <artifactId>zookeeper</artifactId>
                    <version>${version.zookeeper.tar.gz}</version>
                    <type>tar.gz</type>
                </dependency>
            </dependencies>
            <build>
                <plugins>
                    <plugin>
                        <artifactId>maven-dependency-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>copy-accumulo-tarball</id>
                                <goals>
                                    <goal>copy</goal>
                                </goals>
                                <phase>process-resources</phase>
                                <configuration>
                                    <artifact>gov.nsa.datawave.quickstart:accumulo:${version.accumulo.tar.gz}:tar.gz</artifact>
                                    <outputDirectory>${project.basedir}/../bin/services/accumulo</outputDirectory>
                                </configuration>
                            </execution>
                            <execution>
                                <id>copy-hadoop-tarball</id>
                                <goals>
                                    <goal>copy</goal>
                                </goals>
                                <phase>process-resources</phase>
                                <configuration>
                                    <artifact>gov.nsa.datawave.quickstart:hadoop:${version.hadoop.tar.gz}:tar.gz</artifact>
                                    <outputDirectory>${project.basedir}/../bin/services/hadoop</outputDirectory>
                                </configuration>
                            </execution>
                            <execution>
                                <id>copy-wildfly-tarball</id>
                                <goals>
                                    <goal>copy</goal>
                                </goals>
                                <phase>process-resources</phase>
                                <configuration>
                                    <artifact>gov.nsa.datawave.quickstart:wildfly:${version.wildfly.tar.gz}:tar.gz</artifact>
                                    <outputDirectory>${project.basedir}/../bin/services/datawave</outputDirectory>
                                </configuration>
                            </execution>
                            <execution>
                                <id>copy-zookeeper-tarball</id>
                                <goals>
                                    <goal>copy</goal>
                                </goals>
                                <phase>process-resources</phase>
                                <configuration>
                                    <artifact>gov.nsa.datawave.quickstart:zookeeper:${version.zookeeper.tar.gz}:tar.gz</artifact>
                                    <outputDirectory>${project.basedir}/../bin/services/accumulo</outputDirectory>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
