spring:
  application:
    name: querymetric
  cloud:
    bus:
      enabled: false
    consul:
      enabled: false
    config:
      enabled: false
      discovery:
        enabled: false
  main:
    banner-mode: "OFF"

datawave:
  table:
    cache:
      enabled: false
