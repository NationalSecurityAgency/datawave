package datawave.experimental.fi;

import java.util.Set;

import datawave.util.TableName;

public class SequentialUidScannerTest
// extends UidScannerTest
{

    // protected void test(String query, Set<String> expected) {
    // SequentialUidScanner scan = new SequentialUidScanner(util.getClient(), util.getAuths(), TableName.SHARD, "scanId");
    // test(scan, query, expected);
    // }
}
