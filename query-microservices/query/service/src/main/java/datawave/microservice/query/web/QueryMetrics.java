package datawave.microservice.query.web;

import datawave.webservice.query.metric.BaseQueryMetric;
import org.springframework.stereotype.Component;

// TODO: Remove this placeholder and replace it with the real thing once it's ready.
@Component
public class QueryMetrics {
    
    public void updateMetric(BaseQueryMetric baseQueryMetric) {
        System.out.println(baseQueryMetric);
    }
}
