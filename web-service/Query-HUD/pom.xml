<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>nsa.datawave.webservice</groupId>
        <artifactId>WebService</artifactId>
        <version>4.3.0-SNAPSHOT</version>
    </parent>
    <artifactId>Query-HUD</artifactId>
    <packaging>war</packaging>
    <name>Query-HUD</name>
    <url>http://maven.apache.org</url>
    <properties>
        <datawave.root>${project.parent.parent.basedir}</datawave.root>
        <phantomjs.outputDir>${project.build.directory}/phantomjs</phantomjs.outputDir>
    </properties>
    <dependencies>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>3.8.1</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <build>
        <finalName>Query-HUD</finalName>
        <plugins>
            <plugin>
                <groupId>com.github.klieber</groupId>
                <artifactId>phantomjs-maven-plugin</artifactId>
                <version>0.7</version>
                <configuration>
                    <version>1.9.8</version>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>install</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>com.github.searls</groupId>
                <artifactId>jasmine-maven-plugin</artifactId>
                <version>1.3.1.6</version>
                <extensions>true</extensions>
                <configuration>
                    <preloadSources>
                        <!-- This fixes Ext is not defined error -->
                        <source>${project.build.directory}/webjars/META-INF/resources/webjars/extjs/4.2.0/ext-all-debug.js</source>
                    </preloadSources>
                    <jsSrcDir>${basedir}/src/main/webapp</jsSrcDir>
                    <jsTestSrcDir>${basedir}/src/test</jsTestSrcDir>
                    <sourceIncludes>
                        <include>app/model/**/*.js</include>
                        <include>app/store/**/*.js</include>
                        <include>app/view/**/*.js</include>
                        <include>app/controller/**/*.js</include>
                        <include>hudapp.js</include>
                    </sourceIncludes>
                    <specIncludes>
                        <include>spec/**/*.js</include>
                    </specIncludes>
                    <specDirectoryName>spec</specDirectoryName>
                    <customRunnerTemplate>src/test/config/ExtJsSpecRunner.htmltemplate</customRunnerTemplate>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>test</goal>
                        </goals>
                        <configuration>
                            <webDriverClassName>org.openqa.selenium.phantomjs.PhantomJSDriver</webDriverClassName>
                            <webDriverCapabilities>
                                <capability>
                                    <!-- phantomjs.binary is set by the phantomjs-maven-plugin when it downloads the artifact -->
                                    <name>phantomjs.binary.path</name>
                                    <value>${phantomjs.binary}</value>
                                </capability>
                            </webDriverCapabilities>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>unpack-webjars</id>
                        <goals>
                            <goal>unpack</goal>
                        </goals>
                        <phase>generate-sources</phase>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>org.webjars</groupId>
                                    <artifactId>extjs</artifactId>
                                    <version>4.2.0</version>
                                    <type>jar</type>
                                    <outputDirectory>${project.build.directory}/webjars</outputDirectory>
                                    <includes>META-INF/resources/**</includes>
                                    <excludes>**/builds/**,**/cmd/**,**/docs/**,**/examples/**,**/packages/**,**/plugins/**,**/sencha/**,**/src/**,**/welcome/**,**/build.xml,**/index.html,**/release-notes.html</excludes>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <configuration>
                    <webResources>
                        <resource>
                            <directory>${project.build.directory}/webjars/META-INF/resources/webjars</directory>
                        </resource>
                    </webResources>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
