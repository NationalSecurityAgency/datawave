spring:
  application:
    name: audit-test
  cloud:
    bus:
      enabled: false
    discovery:
      enabled: false
    consul:
      enabled: false
    config:
      discovery:
        enabled: false
